# Keyless TypeScript SDK replay patch over the live profile patch.

- id: llm-deepseek
  name: '@mutantcat/dsh-llm-deepseek'
  disabled: true

- insert:
    - id: llm-replay
      name: '@mutantcat/dsh-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
