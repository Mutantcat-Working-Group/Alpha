# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: llm-deepseek
  name: '@mutantcat/dsh-llm-deepseek'
  disabled: true

- insert:
    - id: lsp
      name: '@mutantcat/dsh-lsp'
    - id: lsp-stdio
      name: '@mutantcat/dsh-lsp-stdio'
      config:
        servers:
          fixture:
            command: !!js process.execPath
            args: ['./lsp-server.mjs']
            extensionToLanguage:
              '.ts': typescript
    - id: tool-lsp
      name: '@mutantcat/dsh-tool-lsp'
      config:
        maxLocations: 1
    - id: llm-replay
      name: '@mutantcat/dsh-llm-replay'
      config:
        providers:
          - id: deepseek-official
            name: DeepSeek
            models:
              - id: deepseek-v4-flash
              - id: deepseek-v4-pro

- id: timeout-policy
  name: '@mutantcat/dsh-tool-call-timeout-policy'

- id: plugin-package-inventory-deepseek
  disabled: true
