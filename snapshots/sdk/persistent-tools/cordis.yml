# Per-launch SDK customization: retain only persistent Bash and the string
# editor as model-facing tools, with process-local providers and an explicit
# danger-full-access / never-ask policy.

- insert:
    - id: tool-str-replace-editor
      name: '@mutantcat/dsh-tool-str-replace-editor'
      config:
        maxOutputChars: 16000

- id: llm-deepseek
  name: '@mutantcat/dsh-llm-deepseek'
  config:
    apiKeyEnv: DEEPSEEK_API_KEY
    streamIdleTimeoutMs: 172800000
    models:
      - id: !!js process.env.DSH_MODEL ?? 'deepseek-v4-flash'
        contextWindow: !!js Number(process.env.DSH_CONTEXT_WINDOW ?? 1000000)

- id: sandbox-policy
  name: '@mutantcat/dsh-sandbox-policy'
  config:
    mode: danger-full-access
    workspaceRoot: !!js process.cwd()

- id: approval
  name: '@mutantcat/dsh-user-approval'
  config:
    policy: never

- id: permission
  name: '@mutantcat/dsh-permission-presets'
  disabled: true

- id: bash-sandbox
  name: '@mutantcat/dsh-bash-sandbox'
  disabled: true

- id: pwsh-sandbox
  name: '@mutantcat/dsh-pwsh-sandbox'
  disabled: true

- id: fs-sandbox
  name: '@mutantcat/dsh-fs-sandbox'
  disabled: true

- id: tool-bash
  name: '@mutantcat/dsh-tool-bash'
  disabled: true

- id: tool-pwsh
  name: '@mutantcat/dsh-tool-pwsh'
  disabled: true

- id: agent-instructions
  name: '@mutantcat/dsh-agent-instructions'
  disabled: true

- id: skill
  name: '@mutantcat/dsh-skill'
  disabled: true

- id: skill-filesystem
  name: '@mutantcat/dsh-skill-filesystem'
  disabled: true

- id: tool-skill
  name: '@mutantcat/dsh-tool-skill'
  disabled: true

- id: tool-jobs
  name: '@mutantcat/dsh-tool-jobs'
  disabled: true

- id: tool-fs
  name: '@mutantcat/dsh-tool-fs'
  disabled: true

- id: tool-fs-search
  name: '@mutantcat/dsh-tool-fs-search'
  disabled: true

- id: tool-subagent-control
  name: '@mutantcat/dsh-tool-subagent-control'
  disabled: true

- id: tool-subagent-list-agents
  name: '@mutantcat/dsh-tool-subagent-control/list-agents'
  disabled: true

- id: tool-subagent
  name: '@mutantcat/dsh-tool-subagent'
  disabled: true

- id: tool-subagent-fork
  name: '@mutantcat/dsh-tool-subagent'
  disabled: true

- id: tool-workflow
  name: '@mutantcat/dsh-tool-workflow'
  disabled: true

- id: tool-todo
  name: '@mutantcat/dsh-tool-todo'
  disabled: true

- id: tool-goal
  name: '@mutantcat/dsh-tool-goal'
  disabled: true

- id: tool-ralph
  name: '@mutantcat/dsh-tool-ralph'
  disabled: true

- id: tool-web
  name: '@mutantcat/dsh-tool-web'
  disabled: true

- id: plan-mode
  name: '@mutantcat/dsh-plan-mode'
  disabled: true

- id: system-prompt
  name: '@mutantcat/dsh-system-prompt'
  config:
    includeHarnessIdentity: false
    personaPrefix: !!js process.env.DSH_SYSTEM_PROMPT ?? 'You are a helpful software engineer assistant.'

- id: session-persistence-jsonl
  name: '@mutantcat/dsh-session-persistence-jsonl'
  config:
    root: !!js dshHomePath('sessions')
    compression: none

- insert:
    - id: bash-local
      name: '@mutantcat/dsh-bash-local'

    - id: fs-local
      name: '@mutantcat/dsh-fs-local'
      config:
        cwd: !!js process.cwd()

    - id: terminal
      name: '@mutantcat/dsh-terminal'

    - id: terminal-bash
      name: '@mutantcat/dsh-terminal-bash'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@mutantcat/dsh-tool-bash-persistent'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

- id: session-log-deepseek
  config:
    enabled: false
